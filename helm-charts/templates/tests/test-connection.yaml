apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "regsync.fullname" . }}-test-connection"
  labels:
    {{- include "regsync.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "regsync.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
