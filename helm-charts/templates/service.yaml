apiVersion: v1
kind: Service
metadata:
  name: {{ include "regsync.fullname" . }}
  labels:
    {{- include "regsync.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "regsync.selectorLabels" . | nindent 4 }}
